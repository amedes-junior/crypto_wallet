<%= form_with(model: mining_type, local: true) do |form| %>
  <% if mining_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mining_type.errors.count, "error") %> prohibited this mining_type from being saved:</h2>

      <ul>
      <% mining_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Descrição" %>
    <%= form.text_field :description, class:"form-control", placeholder:"Informe a Descrição" %>
  </div>

  <div class="form-group">
    <%= form.label "Acrônimo" %>
    <%= form.text_field :acronym, class:"form-control", placeholder:"Informe o Acrônimo" %>
  </div>

  <div class="actions">
    <%= form.submit nil, class:"btn btn-primary" %>
  </div>
<% end %>
